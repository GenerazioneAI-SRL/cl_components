/// Base class for every route entry consumed by a [Module].
///
/// Concrete subtypes: [ChildRoute], [ModuleRoute], [ShellModularRoute].
abstract class ModularRoute {
  /// Creates a modular route.
  ModularRoute();
}
